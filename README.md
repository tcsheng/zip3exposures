# zip3exposures

R package for estimating ZIP3-level daily PM2.5 exposure using county-weighted US EPA AirMonitor data.

# Installation

```r
# install.packages("devtools")
devtools::install_github("tcsheng/zip3exposures")
